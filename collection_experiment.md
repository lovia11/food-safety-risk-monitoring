# 单商品图片采集对比实验

> **Standalone 验收说明（后补）**：本报告中的真实页面数据由 Codex 浏览器调试能力辅助采集，用于证明 DOM、页面资源和截图三种路线的可行性。项目已据此编写 `src/phase1_experiment.py`，但当时及当前环境均未安装 Python Playwright，尚未在完全关闭 Codex 的普通终端中完成真实单商品复跑。因此，本报告属于“页面侦察与方法验证”，不能作为 standalone Playwright 采集器已经验收的证明。

## 结论

Phase 1 的真实单商品采集路线实验已完成。对本次页面，推荐 MVP 使用：

**DOM 详情容器定位为主 → 浏览器已观察资源导出/Playwright Network 响应补漏 → 分段与全页截图留证。**

DOM 是建立“图片属于当前商品详情”关联最可靠的依据；Network 单独使用噪声很高；截图适合人工复核与兜底，不应作为 OCR 主输入。

## 实验对象与环境

- 实验时间：2026-08-17 17:12～17:16（Asia/Shanghai）
- 关键词：酸枣仁
- 平台：天猫
- 商品 ID：`606232126144`
- 商品：北京同仁堂酸枣仁膏古方桂圆百合茯苓茶桑葚阿胶膏方官方旗舰店
- 店铺：同仁堂官方旗舰店
- 页面地区信息：山西省太原市
- 浏览器：可见 Google Chrome `151.0.7922.138`，已登录
- 系统：Windows NT `10.0.22631.0`
- Python：`3.10.0`
- Python Playwright：未安装（依赖下载被本机代理/TLS 阻断）
- PaddleOCR / PaddlePaddle：未安装；本次严格停在 Phase 1

真实商品 URL、完整图片 URL、哈希、尺寸和采集方法见 [`meta.json`](output/20260817T171318/products/606232126144/meta.json)。

## 实际 DOM 结构发现

没有预设淘宝 DOM。连接真实页面后实际发现：

- 页签列表：`#titleTabs`
- “图文详情”页签的本次类名：`.tabTitleItem--z4AoobEz`
- 图文详情正文：`#imageTextInfo-container`
- 正文图片的本次类名：`.descV8-singleImage-image`
- 正文容器包含 17 张 `<img>` 和 9 个链接

点击“图文详情”后，活动页签文本变为“图文详情”，页面滚动到 `scrollY=2374`，正文容器顶部位于视口约 99 px 处。正文容器当时高度约 23,083 px。

## 三种策略真实结果

| 指标 | DOM 图片 | Network / 浏览器资源 | 截图 |
| --- | --- | --- | --- |
| 获取数量 | 页面共 50 个 `<img>`、22 个 CSS 背景；正文容器精确命中 17 张 | 观察到 108 个唯一图片资源；17 个与正文 DOM URL 精确匹配并成功导出 | 1 张概览、6 张分段、1 张全页，共 8 张 |
| 是否容易对应当前商品 | 高；17 张均位于 `#imageTextInfo-container` | 单独使用较差；需用 DOM URL 过滤，过滤后 17/17 对应 | 上下文明确，但难建立单图级 URL/来源关系 |
| 图片清晰度 | 原图 790×778～790×1493，另有 800×800、1125×1446 | 与 DOM 匹配的资源保持原始尺寸，无页面缩放损失 | 分段为 1692×868；全页为 1692×28145，文字被页面缩放且混入 UI |
| 噪声数量 | 17 张中人工抽查发现 2 张明显不宜 OCR：关联商品九宫格、平台价格说明 | 91/108 不在正文 DOM URL 集合中，包含主图、SKU、推荐、头像、Logo、平台资源等 | 页头、页签、悬浮栏、推荐区等均成为 OCR 噪声 |
| 是否受懒加载影响 | 本次连接时 17 张均已有 `src/currentSrc` 和自然尺寸 | 点击和完整滚动前后图片资源数都为 108；本次未新增图片资源 | 页面由 25,818 px 增长到 28,145 px，说明底部内容会随滚动扩展 |
| 是否容易保存 | URL 提取容易；原始响应格式需要按 MIME 而不是 URL 后缀保存 | 17 张匹配资源全部导出成功，0 失败，耗时约 25.4 秒 | 容易保存，但文件大且上下文与图片内容混合 |
| OCR 适用性 | 高；15 张正文宣传/包装候选可直接进入后续 OCR | DOM 过滤后高；不经过 DOM 过滤则很低 | 中低，只作为 fallback 和人工证据 |

### DOM 结果

17 张正文图片全部存在 `src` 和 `currentSrc`，没有使用 `srcset` 或 `data-src`。其中 16 个 URL 以 `.jpg` 结尾，但真实返回 MIME 是 `image/webp`；另 1 张为 `image/png`。保存程序因此使用真实 MIME：

- WebP：16 张
- PNG：1 张
- 总大小：1,658,660 bytes
- 保存失败：0

所有图片均保存了 SHA-256、原始 URL、真实 MIME、自然尺寸、DOM 属性和资源 ID。Pillow 二次读取验证 17 张文件全部有效。

正文容器只能证明图片属于当前页面的“图文详情内容”，不能保证每张都是当前商品宣传图。视觉抽查发现：

- `original_001.webp` 是关联商品九宫格；
- `original_017.png` 是平台价格说明；
- 中间图片抽查可见品牌说明、食用场景、原料和包装宣传，适合后续 OCR。

因此 Phase 2 的首批 OCR 候选为 `original_002`～`original_016`，但最终仍应保留可追溯清单，不删除原始证据。

### Network / 浏览器资源结果

浏览器资源清单中 108 个图片资源都带有 `resource` 来源；其中：

- 38 个同时来自 DOM 图片属性；
- 9 个同时来自 CSS `background-image`；
- 17 个 URL 与正文 DOM 图片精确匹配；
- 91 个为正文之外的资源候选噪声。

本次浏览器控制接口可以导出已观察资源并返回 Content-Type，但不暴露完整 HTTP 响应头，因此 `Content-Length` 响应头记为 `null`，改为记录实际保存字节数。可复现 Python 工具已实现真正的 Playwright `response` 监听、响应头记录和 `response.body()` 保存；缓存漏捕时才使用共享 Cookie 的 `BrowserContext.request` 补取，不使用独立 `requests` 会话。

### 截图结果

- 概览：1692×868
- 分段截图：6 张，均为 1692×868
- 全页：1692×28145，2,977,321 bytes

分段截图可定位宣传图在商品详情中的上下文，全页截图适合演示与人工复核。但全页图过长，若直接 OCR 会缩放文字、混入页面 UI，也难以追溯到单张原始图片 URL。

## 图片筛选阈值

本次真实详情图的最小尺寸为 790×778，所以第一版尺寸规则采用：

`natural_width >= 700 and natural_height >= 700`

该阈值能排除小图标和头像，但不能排除关联商品图与平台说明。还必须结合：

1. 是否位于实际发现的详情容器；
2. 是否被其他商品链接/热区包围；
3. 是否为平台统一说明图；
4. Phase 2 OCR 后是否出现酸枣仁、配料、睡眠、助眠等业务词。

## 懒加载观察

初始、点击图文详情后、滚动到底后三次资源清单均为 108 张图片，17 张详情图从开始就已经加载。因此本次商品页面没有观察到“滚动才新增详情图片”的现象。

但页面总高度从 25,818 px 增加到 28,145 px，表明底部推荐/其他内容仍受滚动影响。这个单商品结果不能推广为淘宝所有页面都不使用图片懒加载，批量程序仍必须保留点击、滚动、等待和滚动后复查。

## 实际问题与限制

1. 初次连接时 Chrome 扩展和本机连接组件缺失；人工重新安装插件后恢复。
2. 浏览器资源导出接口不提供完整响应头，无法验证服务器的 `Content-Length`；已记录实际文件字节数，并在 Python Playwright 工具中补上真实响应监听。
3. 项目 `.venv` 已创建，但 PyPI 访问受到本机代理/TLS 限制，Playwright 包未能下载；辅助函数与现有产物验证仍已运行通过。

实验期间没有遇到淘宝登录、短信验证、验证码或异常访问拦截，也没有采取任何绕过措施。

## 输出证据

- [`meta.json`](output/20260817T171318/products/606232126144/meta.json)：商品、图片、URL、尺寸、哈希与截图关联
- [`dom_text.txt`](output/20260817T171318/products/606232126144/dom_text.txt)：直接获取的 DOM 文本
- [`dom_images.json`](output/20260817T171318/products/606232126144/manifests/dom_images.json)：DOM 图片属性、尺寸、位置与分类
- [`network_inventory.json`](output/20260817T171318/products/606232126144/manifests/network_inventory.json)：108 个资源记录
- [`original_images.json`](output/20260817T171318/products/606232126144/manifests/original_images.json)：17 张保存原图清单
- [`page/`](output/20260817T171318/products/606232126144/page)：概览、分段和全页截图
- [`images/original/`](output/20260817T171318/products/606232126144/images/original)：17 张真实高清原图

## 下一阶段门槛

本报告完成后才进入 Phase 2。下一步应先解决 PaddleOCR 安装，然后只对 `original_002`～`original_016` 运行 OCR，逐张保存文本和结构化结果；截图仅在原图 OCR 失败时使用。
